# Session Log — 2026-07-08

## What Was Accomplished
- Moved the feature allowlist from `config/allowlist.txt` (baked into the Docker image, unfit for runtime edits) to a Postgres `allowlist` table. Removed the `*` wildcard — access is now invite-only.
- Built an admin UI (`/admin/allowlist`) and API (`GET/POST /api/admin/allowlist`, `DELETE .../[email]`) for managing access without a redeploy.
- Fixed `.github/workflows/deploy.yml`'s Cloud Run deploy step, which was launching the service with zero env vars configured even before this session.
- Diagnosed and fixed a real IAM gap on `r-and-d-489319`: `ori@compulocks.com` was missing `serviceusage.serviceUsageAdmin`, `storage.admin`, and `logging.viewer`. Self-granted each, with explicit user authorization every time, logged in `gcp-migration/IAM-CHANGE-LOG.md`.
- Migrated plaintext `AUTH_SECRET`/`GOOGLE_API_KEY`/`POSTGRES_URL` off the live Cloud Run container spec into Secret Manager.
- Built and deployed current `main` (commit `e02c6d17` → later `f4baed39`) to Cloud Run via `gcloud builds submit` (using a self-created staging bucket after the project's default one proved inaccessible).
- Found and fixed a production bug: the `POSTGRES_URL` secret had literal double-quote characters carried over from `.env.local`, causing `pg` to fail DNS resolution (`getaddrinfo EAI_AGAIN base`) on every DB-backed route. Verified the fix on an isolated 0%-traffic Cloud Run revision before cutting production traffic over.
- Verified end-to-end: `/`, `/one-pager`, `/admin/allowlist`, `/api/documents`, `/api/admin/allowlist` all return 200 on `https://mrd-producer-269404192985.us-central1.run.app`.
- Walked through a full interactive design (visual artifact + confirmed decisions) for moving One-Pager/PRD document storage from Postgres to Google Cloud Storage. Not implemented — captured as a decided design for the next session.
- Updated all four quartet files (CLAUDE.md, CHANGELOG.md, TODO.md, memory) plus a full narrative session log (`gcp-migration/06-2026-07-08-session-log.md`) and compacted `MEMORY.md` from 253 lines to 42 (moved cross-cutting lessons to `memory/lessons-learned.md`).

## Decisions Made
- **Invite-only allowlist, no wildcard** — explicit user decision, since guest access ("Shmuel, Shahar, me") was described as invite-based, not open.
- **Admin UI over manual file edits** for allowlist management — required moving storage to Postgres since Cloud Run's image is immutable.
- **Cloud SQL dropped mid-session** in favor of Google Cloud Storage for One-Pager/PRD document JSON — user's call, driven by wanting to detach PRD/One-Pager storage from a relational DB. Full design decided (bucket layout, filename convention, envelope shape, concurrency model) — see `memory/gcs-document-storage-design.md`.
- **MRD is explicitly out of scope** for the GCS work and for "getting things working" in general — it isn't released yet. One-Pager is the only tool required to work end-to-end right now.
- **IAM self-grants over waiting on Danny** (who has `roles/owner` on the project) — user chose speed, each grant confirmed explicitly before being made, all logged.
- **WIF/CI-CD setup NOT done** — correctly held back by the sandbox as a distinct sensitive action (new GitHub↔GCP trust relationship) requiring its own sign-off, separate from the IAM self-grants already authorized.
- **Real OAuth NOT set up** — deferred, since it requires a manual, human-only step in GCP Console (`gcloud` cannot create OAuth 2.0 clients).

## State at Handoff
- **Phase**: GCP migration — core deployment done and verified; two follow-on workstreams open (real OAuth + CI/CD; GCS document storage).
- **Blockers**: none active. Everything currently blocked needs either a human Console click (OAuth client) or a design-to-spec step (GCS) — no external dependency is being waited on right now.
- **Next action**: Write the GCS document storage design (already fully decided, see `memory/gcs-document-storage-design.md`) to `docs/superpowers/specs/YYYY-MM-DD-gcs-document-storage-design.md` using the brainstorming skill's spec-writing step, get the user to confirm it in writing, then invoke `writing-plans` for an implementation plan. Do not re-ask any of the design decisions already recorded.

## Files Modified This Session
- `lib/db-migrations/004-allowlist.sql` — new `allowlist` table, seeded from the old file
- `lib/feature-gate.ts` — rewritten async, Postgres-backed, 30s cache, file fallback for local dev
- `app/admin/allowlist/page.tsx`, `app/admin/allowlist/AllowlistClient.tsx` — new admin UI
- `app/api/admin/allowlist/route.ts`, `app/api/admin/allowlist/[email]/route.ts` — new admin API
- `app/page.tsx`, `app/api/documents/[id]/export/route.ts` — updated for async `feature-gate` calls
- `__tests__/middleware/prd-gate.test.ts` — rewritten to mock `db-client` instead of `fs`
- `config/allowlist.txt` — wildcard removed, marked as local-dev-only fallback
- `.github/workflows/deploy.yml` — `gcloud run deploy` now wires secrets via `--set-secrets`
- `gcp-migration/scripts/06-run-when-unblocked.sh` — new runbook (has stale Cloud SQL sections from before the pivot — needs a trim pass)
- `gcp-migration/IAM-CHANGE-LOG.md` — new, audit trail for every self-granted IAM role this session
- `gcp-migration/06-2026-07-08-session-log.md` — new, full narrative record
- `.claude/launch.json` — new, dev server config for preview tooling
- `CLAUDE.md`, `CHANGELOG.md`, `TODO.md` — quartet updates
- `memory/gcp-migration.md` — rewritten (most of the June 4 content was stale)
- `memory/gcs-document-storage-design.md` — new
- `memory/lessons-learned.md` — new (extracted from MEMORY.md to bring it under the line limit)
- `memory/MEMORY.md` — compacted from 253 → 42 lines
- Live GCP infra changes (not files, but real state): IAM bindings on `r-and-d-489319`, 3 Secret Manager secrets, `gs://r-and-d-489319-cloudbuild-staging` bucket, Cloud Run revision `mrd-producer-00045-vex` serving 100% traffic

## Context Notes
- The Cloud Run service currently runs with `BYPASS_AUTH=true` — every visitor gets a stub `ori@compulocks.com` session. This is intentional for now but should not be forgotten as a loose end; it's gated behind the env var (hardened earlier against being unconditional) but is still effectively no real authentication in production-on-GCP.
- Two commits from this session (`6ae930cd..f4baed39` range on `main`) triggered `deploy.yml` on push each time — it will keep failing at the WIF auth step until that's explicitly set up. This is expected and not a regression; don't "fix" it reflexively without re-confirming the WIF sign-off with the user first.
- `gcp-migration/scripts/06-run-when-unblocked.sh` still has Cloud SQL provisioning steps from before the GCS pivot. They're dead weight now — a future session should trim or replace them, but they don't cause harm sitting there unused.
- Neon and Vercel are both still fully live and untouched — this was a deliberate choice, not an oversight. Don't decommission either without the user explicitly asking, and even then, only after the GCS migration (if it proceeds) is complete and Cloud Run has been stable for a real stretch of time.
- Danny (`danny@compulocks.com`) has `roles/owner` on `r-and-d-489319` and was the originally-identified alternate path for the IAM grants that were ultimately self-granted instead — worth knowing he's a fallback if a future permission gap can't be self-granted (e.g. `roles/owner` itself, which was never requested or granted to ori).
